All files that will be put into the /cdrom directory will be accessible inside of the windows box.

Important notes:
- You need twice as much storage - .ISO image will be generated and stored in /cdrom.iso.

